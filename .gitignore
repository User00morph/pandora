# Sensitive personal data — D-6 decision (SNRS PRD §9, locked 2026-04-24)
D.P.S.A/natal/dpsa_natal_morph_birthdata.md
m.d

# Ministry founding documents — real names, officer structure (local only, never push)
D.O.M/house-of-auset/

# Client documents — real names, agreements, personal data (local only, never push)
D.S.E/clients/

# Personal legal documents — disputes, personal identifying info (local only, never push)
D.S.E/documents/dse_doc_*

# Trust documents — real names, DOBs, addresses (local only, never push)
D.S.E/documents/trusts/

# Raw audio/video data — too large for GitHub, stored locally only
*.mp4
*.mp3
*.m4a
*.wav
*.aac
